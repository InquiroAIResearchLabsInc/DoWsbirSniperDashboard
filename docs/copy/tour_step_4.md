# tour_step_4

<!-- Guided tour, step 4 of 4 — the Admin receipt ledger. Body prose only; the
     "STEP 4 OF 4" indicator and the amber prompt are rendered by tour.js. -->

Every action in this system writes a permanent, timestamped record — every
score, every recommendation, every change. The chain is intact and replayable.
