# tour_step_4

<!-- TODO (Bubba writes): guided tour, step 4 of 4 — the Admin receipt ledger.
     2 sentences max. Intent: every action writes a permanent, timestamped
     record — every score, every recommendation, every change — and the chain
     is intact and replayable. The "STEP 4 OF 4" indicator and the amber
     "Click Admin to see the ledger" prompt are rendered by tour.js — this atom
     is body prose only. -->

<PLACEHOLDER_TOUR_STEP_FOUR>
